// Given an array const names = ['Alice', 'Bob', 'Charlie', 'Dave'];, create a new array that is a copy of the original array using slice().

const names  = ['Alice', 'Bob', 'Charlie', 'Dave'];

const cpoy = names.slice(0) 
console.log(`Original array is [${names}] and copied array is "${cpoy}"`)