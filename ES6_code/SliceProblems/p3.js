// Given an array const colors = ['red', 'green', 'blue', 'yellow'];, use slice() to create a new array that excludes the first element.

const colors = ['red', 'green', 'blue', 'yellow']

const newColors = colors.slice(1)

console.log(newColors)